upstream frontend {
  server frontend:3000;
}

upstream backend {
  server backend:4000;
}

server {
  listen 80;
  server_name _;

  # --- API ---
  # Proxy /api -> backend, en retirant le préfixe /api
  location /api/ {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffering off;           # important pour SSE
    proxy_read_timeout 1h;         # SSE longue durée
    proxy_pass http://backend/;    # le slash final remplace /api/ par /
    client_max_body_size 20m;
  }

  # --- Magic links courts ---
  # Proxy /m/<id> directement vers le backend pour rédemption et pose de cookie
  location ^~ /m/ {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_pass http://backend;     # sans slash pour conserver le chemin /m/<id>
  }

  # --- Frontend ---
  location / {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_pass http://frontend;
  }
}
